class Solution {
public:
    int strStr(string haystack, string needle) {
     
        if (needle == "")
            return 0;
        int tmp;
        if ((tmp = haystack.find(needle) )!= std::string::npos)
            return tmp;
        else
            return -1;
       
    }
};
